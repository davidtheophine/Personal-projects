# Copy to .env and fill in. Required only for the REAL Spotify playback/save path.
# The interaction demo works fully without any of this (simulated playback + local likes).
VITE_SPOTIFY_CLIENT_ID=your_client_id_here
VITE_SPOTIFY_REDIRECT_URI=http://127.0.0.1:5173/callback
